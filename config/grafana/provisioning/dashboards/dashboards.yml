apiVersion: 1

providers:
  - name: "Basket API Dashboards"
    orgId: 1
    folder: "Basket API"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
